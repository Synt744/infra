---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Enable backup timer
  ansible.builtin.systemd:
    name: restic-backup.timer
    state: started
    enabled: true
